import * as joint from "jointjs";

export const AdaptiveCardModel = joint.dia.Element.define('AdaptiveCardModel', { // not used for now test.shape
  size: { width: 300, height: 0 },
  z: 1,
  template: null,
  border: '2px solid #ddd',
});